-- 스냅샷 정렬 전용 — 스키마를 바꾸지 않는다.
--
-- 왜 필요한가: `drizzle/meta/` 의 스냅샷이 0003 에서 멈춰 있었다. 0004~0006 은 손으로 쓴
-- SQL 이라 스냅샷을 남기지 않았고, 그 상태에서 `drizzle-kit generate` 를 돌리면
-- **0003 과 현재 스키마의 차이 전체**를 새 마이그레이션으로 뱉는다. 실제로 돌려 본 결과:
--
--   ALTER TABLE "npcs" DROP COLUMN "openclaw_config";
--
-- 0005 는 같은 삭제를 하되 **그 전에** 페르소나를 `agent_config` 로 옮기고 레거시 행을
-- 백업 테이블에 남긴다. 생성된 쪽에는 그 단계가 없고 `IF EXISTS` 도 없다. 읽지 않고
-- 적용하면 페르소나가 사라진다.
--
-- 이 파일은 SQL 을 실행하지 않는다. 짝이 되는 `meta/0007_snapshot.json` 이 현재
-- `src/db/schema.ts` 를 그대로 담고 있어, 이후 `generate` 는 여기서부터 차분을 잡는다.
-- 0004~0006 의 실제 변경은 각자의 파일이 이미 수행한다(신규 DB 도 순서대로 적용된다).
SELECT 1;
