# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build
/.artifacts/
*.tgz

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Agent/tool working directories; ignored files may still be unique, so inspect before removing a worktree.
.superpowers/
# .github 은 개발 메타(.claude/.codex 등)와 함께 묶여 무시됐지만, workflows 는
# 저장소 인프라다 — 원격에 파일이 없으면 CI 가 아예 돌지 않는다.
.github/*
!.github/workflows/
.claude/
.codex/
.agents/
.gemini/
/.dryforge/
# Local development tools only (including scripts/local/backlog/); not product code.
scripts/local/
# Local tool data, including dev-backlog.sqlite and its WAL/SHM sidecars.
data/
db.sqlite
:memory-file-not-used:*
*.db
*.db-shm
*.db-wal
# Public Git tree must not contain developer-only instructions or plans.
docs/
AGENTS.md
CLAUDE.md
GEMINI.md
e2e/README.md
assets/lpc-office-tilesets/
public/assets/built-in/downloads/
tmp/*
!tmp/.gitkeep
tests/
# 예전 배포 레시피 폴더(삭제됨). 로컬에 남은 파일이 드러나지 않게 계속 무시한다.
deploy/

# Playwright
/test-results/
/playwright-report/
/blob-report/

# remember 플러그인 런타임 로그·pid — 저장소 산출물이 아니다
.remember/

# Character editing sources (Blender, supplied bases, review captures) — not shipped
/art/
__pycache__/
*.py[cod]

# --- crew-office ---
# phase0 실험 부산물: 가짜 HOME(인증 파일 생길 수 있음), CLI 작업 폴더
spikes/phase0/fakehome/
spikes/phase0/work/
